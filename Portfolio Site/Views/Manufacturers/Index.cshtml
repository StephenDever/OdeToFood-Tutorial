@model IEnumerable<Portfolio_Site.Models.BicycleManufacturer>

@{
    ViewBag.Title = "Index";
}

<h2>Bicycle Archive</h2>

Showing @Model.Count() archives.

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@foreach (var item in Model)
{
    <div class="model">
        <h4>@item.Manufacturer - @item.Model</h4>
        <span>Rating: @item.Rating</span>
        <p>Year: @item.Year</p>
        <p>Info: @item.Info</p>

        <span class="right">
            @Html.ActionLink("Edit", "Edit", new { id = item.Id })
        </span>
    </div>
}
